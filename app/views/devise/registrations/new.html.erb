<div class="background_sign_up">
  <div class="container">
    <div class="form_signup">
      <h2 class="titre_signup">Tropicalise toi</h2>

      <%= simple_form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
      <%= f.error_notification %>

      <div class="form-inputs">
        <div class="text_style">
        <%= f.input :username, label: 'Ton pwénom', required: true, autofocus: true %>
        <%= f.input :email, required: true, autofocus: true %>
        <%= f.input :password, label:'Le code secwet', required: true, hint: ("#{@minimum_password_length} characters minimum" if @minimum_password_length) %>
        <%= f.input :password_confirmation,label:'Encowe le code', required: true %>
      </div>
      </div>
      <div class="form-actions">
        <%= f.button :submit, "On y va doudou", class: 'submit_button' %>
      </div>
      <% end %>
      <%= render "devise/shared/links" %>
    </div>
  </div>
</div>
